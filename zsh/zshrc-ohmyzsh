ZSH=$HOME/.oh-my-zsh

ZSH_THEME="nicoulaj"

plugins=(compleat fabric pip git git-flow)

source $ZSH/oh-my-zsh.sh

source $HOME/.dotfiles/zsh/aliases.zsh

if [ -f ~/.zshrc-local ]; then
   source ~/.zshrc-local
fi
