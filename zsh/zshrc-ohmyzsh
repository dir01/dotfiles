ZSH=$HOME/.oh-my-zsh

ZSH_THEME="nicoulaj"

plugins=(compleat git git-flow brew pip sudo) 

source $ZSH/oh-my-zsh.sh

source $HOME/dotfiles/zsh/aliases.zsh

if [ -f ~/.zshrc-local ]; then
   source ~/.zshrc-local
fi
